{
  "name": "nyayanest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NyayaNest — A dependency-free static legal website & consultation platform.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs",
    "start": "node scripts/build.mjs",
    "deploy:pages": "npm run build && npx wrangler pages deploy dist --project-name=nyayanest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
