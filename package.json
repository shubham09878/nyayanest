{
  "name": "lawprime",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LAWPRIME — A dependency-free static legal website & consultation platform.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
